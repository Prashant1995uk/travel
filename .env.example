# API base (browser must reach the backend; use full URL if not using Vite proxy)
VITE_API_URL=http://localhost:5000

# Optional: Google Maps JavaScript API key for embedded map (otherwise OpenStreetMap + OSRM is used)
# VITE_GOOGLE_MAPS_KEY=
